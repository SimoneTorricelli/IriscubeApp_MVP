package com.example.iriscubeapp.recycleBankMovement

import android.content.Context
import androidx.recyclerview.widget.RecyclerView

/*
Classe per definire i tipi da costruire nella recycle View

abstract class DataAdapter(
    private val context: Context
) :
    RecyclerView.Adapter<DataAdapter.MovementViewHolder<*>>() {
    private var adapterDataList: List<Any> = emptyList()

    companion object {
        private const val TYPE_FAMILY = 0
        private const val TYPE_FRIEND = 1
        private const val TYPE_COLLEAGUE = 2
    }


}

 */